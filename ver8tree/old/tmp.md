2 Веди в файле (filelist.md) список всех исполняемых (используемых) файлов - файлов, задействованных при запуске index.html, включая загружаемые им файлы, например, vad-basic-ontology_tech_Appendix.ttl.
Все необходимые файлы должны размещаться в https://github.com/bpmbpm/rdf-grapher/tree/main/ver8atree

4 Все вычисления по анализу RDF-данных по возможности проводи через SPQRQL запросы, которые размети в sparql-queries-code.js. Это будет Коллекция SPARQL запросов, используемых в коде, кроме Коллекция SPARQL запросов для формирования справочников.
Коллекция SPARQL запросов для формирования справочников переименуй из sparql-queries.js в sparql-queries-choice.js.


Кнопку Показать перенеси из окна RDF данные или URI: в окно "Результат:", рядом с этим словом.  

Для каждого выпадающего справочника в Smart Design в sparql-queries.js укажи используемый SPARQL и пояснение к нему, включая какие позиции исключаются из списка (выпадающего списка). 

### 1
На будущее: коментарий к схеме процесса, комментрий к мндивиду процесса. Отдельные типы объектов и нотации.
Листы схем (лтст 1:2 + штамп \ исполнитель, время и т.п.) ? переносы с листа на лист схемы. Версия, ведение архива
предшедствующий и последующий. Отдельно Подробный дескрипшен - в отдельное поле или окно с описанием и включением гиперсссылок. 
Нормативные документы 

При создании нового процесса: Вводим название процесса, далее Имя генеририруется через _ По умолчанию ставится NotDefined

2
разработка Семантической системы для ИТ-Архитекторов и не только (BPM).  
Вектор = "классические EA\BPM" + формальная семантика (Linked Data). 
Эскиз (пока VAD, но archimate и т.п. аналогично) начал мастерить тут (https://github.com/bpmbpm/rdf-grapher/tree/main/ver8tree).

Это кстати это к
"Очень быстро я прихожу к пониманию, что никто не понимает, что тут нарисовано и прошу человека перерисовать в какой-то общепринятой нотации. "

Нужно смотреть глубже: онтология, МетаМодель, Редактор МетаМодели, Reasoner и т.п.  
При этом "одно и тоже" можно перерисовывать в разных нотациях или с упрощением онтологии  (пять классов свести к одному, например, в archimate бизнес-процесс, бизнес-функция, бизнес-сервис и т.п. или наоборот развернуть в 15).
https://t.me/c/1120099288/174147

## Problem
Прокомментируй код и скажи почему выдается пустой результат?

**Подробный разбор кода и причина пустого результата**

Код представляет собой веб-страницу, которая демонстрирует работу функции `funSPARQLvalues` из библиотеки `vadlib_sparql.js`. Данные загружаются в формате **TriG** (RDF-квады с префиксами), затем выполняется SPARQL-запрос, но результат оказывается пустым. Ниже я подробно прокомментирую каждую часть кода и объясню, почему результат пустой.

---

### 1. Структура HTML и подключение скриптов
```html
<script src="https://cdn.jsdelivr.net/npm/n3@1.16.2/browser/n3.min.js"></script>
<script src="vadlib_sparql.js"></script>
```
- **N3.js** — библиотека для работы с RDF в JavaScript (парсинг, хранение, сериализация).  
- **vadlib_sparql.js** — пользовательская библиотека, содержащая функцию `funSPARQLvalues` (выполняет SPARQL-запросы синхронно над хранилищем N3.Store).

---

### 2. Инициализация хранилища и данных
```javascript
const store = new N3.Store();

const trigData = `
    @prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
    @prefix ex: <http://example.org/> .

    ex:graph1 {
        ex:Alice rdf:type ex:Person .
        ex:Alice ex:label22 "Alice" .
    }

    ex:graph2 {
        ex:Bob rdf:type ex:Person .
        ex:Bob ex:label22 "Bob" .
    }

    # Default graph
    {
        ex:Charlie rdf:type ex:Person .
    }
`;
```
- Создаётся пустое хранилище `store`.  
- Данные описаны в формате **TriG** — стандартном RDF-формате для наборов квадов (subject, predicate, object, graph).  
- Используются префиксы `rdf:` и `ex:` для сокращения URI.  
- Описаны три графа: два именованных (`ex:graph1`, `ex:graph2`) и один **default graph** (без имени).  
- В каждом графе содержатся тройки. Обратите внимание: в default graph у `ex:Charlie` нет свойства `ex:label22`, только тип.

---

### 3. Парсинг TriG и наполнение хранилища
```javascript
const parser = new N3.Parser({ format: 'text/trig' });
parser.parse(trigData, (error, quad) => {
    if (quad) store.addQuad(quad);
});
```
- Создаётся парсер для формата TriG.  
- Метод `parse()` запускает **асинхронный** разбор строки.  
- Колбэк вызывается для каждого распарсенного квада (`quad`), и квад добавляется в хранилище.  
- **Важно:** парсинг происходит асинхронно, а код после вызова `parse()` выполняется немедленно, **не дожидаясь окончания разбора**.

---

### 4. Установка глобальных переменных
```javascript
window.currentStore = store;
window.currentPrefixes = {
    'rdf': 'http://www.w3.org/1999/02/22-rdf-syntax-ns#',
    'ex': 'http://example.org/'
};
```
- Предположительно, функция `funSPARQLvalues` использует `window.currentStore` как источник данных и `window.currentPrefixes` для разрешения префиксов в SPARQL-запросе.

---

### 5. Отображение исходных данных и SPARQL-запрос
```javascript
document.getElementById('data').textContent = trigData;

const sparqlQuery = `
   prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> 
   prefix ex: <http://example.org/> 

    SELECT ?person ?label WHERE {
        GRAPH ?g {
            ?person rdf:type ex:Person .
            ?person ex:label22 ?label .
        }
    }
`;
document.getElementById('query').textContent = sparqlQuery;
```
- Отображается TriG-строка и текст запроса.  
- SPARQL-запрос использует те же префиксы (синтаксис `prefix` допустим в SPARQL 1.1, хотя стандарт требует `PREFIX` — реализации обычно не чувствительны к регистру).  
- Запрос ищет в **именованных графах** (`GRAPH ?g`) все субъекты типа `ex:Person`, у которых есть свойство `ex:label22`, и возвращает сам субъект и значение метки.  
- По данным ожидается два результата: `ex:Alice` с "Alice" и `ex:Bob` с "Bob".

---

### 6. Выполнение запроса и вывод результата
```javascript
try {
    const results = funSPARQLvalues(sparqlQuery, 'person');
    document.getElementById('result').textContent = 
        results.length === 0 ? 'Нет результатов' : JSON.stringify(results, null, 2);
} catch (e) {
    document.getElementById('result').textContent = 'Ошибка: ' + e.message;
}
```
- Вызывается `funSPARQLvalues` с запросом и именем основной переменной `'person'` (видимо, для форматирования вывода).  
- Результат выводится в элемент `<pre id="result">`.  
- В данном случае результатом будет `'Нет результатов'`.

---

### ❗ Почему результат пустой?

**Основная причина: асинхронность парсинга TriG.**  

Метод `parser.parse()` запускает разбор данных, но не блокирует выполнение последующего кода. К моменту вызова `funSPARQLvalues` хранилище `store` всё ещё **пусто** (или содержит лишь часть данных), потому что добавление квадов происходит асинхронно в колбэке. Поэтому запрос не находит ни одного совпадения.

#### Как убедиться?
Можно добавить вывод количества квадов после парсинга:
```javascript
parser.parse(trigData, (error, quad) => {
    if (quad) store.addQuad(quad);
    else {
        console.log('Парсинг завершён, квадов в хранилище:', store.size);
        // теперь можно выполнять запрос
    }
});
// Запрос, вызванный здесь, увидит пустой store
```

---

### 🔧 Как исправить?

Необходимо дождаться окончания парсинга перед вызовом `funSPARQLvalues`. Так как парсер асинхронный, можно использовать колбэк завершения или обернуть в промис.

**Вариант 1 — через колбэк завершения:**
```javascript
parser.parse(trigData, (error, quad) => {
    if (quad) store.addQuad(quad);
    else {
        // Парсинг окончен, можно выполнять запрос
        window.currentStore = store;
        // ... установка префиксов, отображение данных, выполнение запроса
        const results = funSPARQLvalues(sparqlQuery, 'person');
        document.getElementById('result').textContent = ...;
    }
});
```

**Вариант 2 — с использованием промиса (более современный):**
```javascript
const parsePromise = new Promise((resolve, reject) => {
    const parser = new N3.Parser({ format: 'text/trig' });
    parser.parse(trigData, (error, quad) => {
        if (error) reject(error);
        else if (quad) store.addQuad(quad);
        else resolve();
    });
});

parsePromise.then(() => {
    window.currentStore = store;
    // ... остальной код запроса
});
```

---

### Дополнительные замечания
- В TriG-строке используются относительные URI (например, `ex:Alice`). Они корректно разрешатся через префикс `ex:`.  
- В SPARQL-запросе применён `GRAPH ?g`, поэтому данные из **default graph** (где нет `ex:label22` у `ex:Charlie`) не влияют на результат — это правильно.  
- Если бы в default graph были нужные тройки, они бы не попали в выборку из-за условия `GRAPH ?g`. Чтобы включить и default graph, можно использовать `GRAPH ?g {}` с опциональным или альтернативный паттерн, но это не требуется в данном примере.

---

### Вывод
Код написан логично, но из-за асинхронности парсера запрос выполняется до загрузки данных. Это типичная ошибка при работе с асинхронными операциями в JavaScript. Решение — перенести вызов функции внутрь колбэка завершения парсинга или использовать промисы.

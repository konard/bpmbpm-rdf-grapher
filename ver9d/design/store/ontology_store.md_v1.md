# Организация Quadstore и Загрузка Онтологии в RDF Grapher ver9d

**Дата:** 2026-02-09  
**Версия:** v1.0  
**Связанные issue:** #266, #322, #344  

## Содержание

1. [Текущая организация quadstore](#текущая-организация-quadstore)
2. [Почему vad-basic-ontology.ttl не загружается](#почему-vad-basic-ontologyttl-не-загружается)
3. [Как работает встроенный reasoning без онтологии](#как-работает-встроенный-reasoning-без-онтологии)
4. [Рекомендации по организации quadstore](#рекомендации-по-организации-quadstore)
5. [SPARQL-Driven подход](#sparql-driven-подход)
6. [Что еще нужно загрузить в quadstore](#что-еще-нужно-загрузить-в-quadstore)

---

## Текущая организация quadstore

### Архитектура хранилища

RDF Grapher ver9d использует **единое индексированное хранилище** на основе N3.Store:

```javascript
let currentStore = null;  // N3.Store - основное хранилище всех данных
```

### Структура данных в quadstore

1. **Пользовательские данные**:
   - `vad:ptree` - дерево концептов процессов
   - `vad:rtree` - дерево концептов исполнителей  
   - `vad:t_*` - схемы процессов (VADProcessDia)

2. **Технологические данные**:
   - `vad:techtree` - данные из `vad-basic-ontology_tech_Appendix.ttl`
   - Загружаются при старте приложения через `loadTechAppendix()`

3. **Виртуальные данные**:
   - `vad:vt_*` - вычисляемые параметры (processSubtype)
   - Создаются динамически через reasoner

### Процесс загрузки данных

```javascript
// 1. При старте приложения
document.addEventListener('DOMContentLoaded', function() {
    loadTechAppendix();  // Загрузка tech appendix
});

// 2. При загрузке пользовательских данных
async function parseRdfData() {
    currentStore = new N3.Store();
    // Парсинг пользовательских TriG
    // Добавление в currentStore
}

// 3. После загрузки пользовательских данных
addTechQuadsToStore();  // Добавление tech данных в общий store
```

---

## Почему vad-basic-ontology.ttl не загружается

### Исторические причины

1. **Размер и производительность**:
   - `vad-basic-ontology.ttl` содержит ~900 строк определений классов
   - Увеличивает время загрузки и потребление памяти
   - Замедляет SPARQL-запросы при большом количестве квадов

2. **Разделение данных и схемы**:
   - Онтология — это схема (метамодель), не сами данные
   - Пользовательские данные не должны смешиваться с определениями схемы
   - Исключение онтологии из сохранённых файлов

3. **Версионирование**:
   - Онтология может обновляться независимо от данных
   - Динамическая загрузка при каждом старте приложения

### Текущая стратегия

**Загружается только технологическая часть**:
- `vad-basic-ontology_tech_Appendix.ttl` — загружается в `vad:techtree`
- `vad-basic-ontology.ttl` — НЕ загружается в quadstore

**Почему это работает**:
- JavaScript константы дублируют определения из онтологии
- Валидация происходит через жёстко заданные списки `VAD_ALLOWED_TYPES`
- Reasoning использует императивные алгоритмы вместо онтологического вывода

---

## Как работает встроенный reasoning без онтологии

### Два уровня reasoning

#### 1. Императивный JavaScript (основной)

**Файл**: `2_triplestore/2_triplestore_logic.js`  
**Функция**: `calculateProcessSubtypes()`

```javascript
// Алгоритм вычисления processSubtype
function calculateProcessSubtypes(hierarchy, prefixes) {
    // 1. Если hasParentObj = pNotDefined -> NotDefinedType
    // 2. Если концепт имеет hasTrig -> Detailed*
    // 3. Если концепт НЕ имеет hasTrig -> notDetailed*
    // 4. Проверка вхождения в схему родительского процесса
}
```

#### 2. SPARQL-Driven reasoning (альтернативный)

**Файл**: `11_reasoning/11_reasoning_logic.js`  
**Функция**: `performSemanticReasoning()`

```javascript
// SPARQL CONSTRUCT запросы для вывода
const processMetadataQuery = `
    SELECT ?process ?parentObj ?hasTrig ?label WHERE {
        GRAPH vad:ptree {
            ?process rdf:type vad:TypeProcess .
            OPTIONAL { ?process vad:hasParentObj ?parentObj }
            OPTIONAL { ?process vad:hasTrig ?hasTrig }
        }
    }
`;
```

### Почему reasoning работает без онтологии

1. **Жёстко заданные правила**:
   - Правила вывода определены в коде, не в онтологии
   - Типы процессов заданы константами в JavaScript

2. **Прямые SPARQL-запросы**:
   - Reasoning использует SPARQL для извлечения данных
   - Логика вывода реализована в JavaScript, а не через RDFS/OWL

3. **Отсутствие необходимости в онтологии**:
   - Все классы и предикаты известны заранее
   - Валидация происходит через списки разрешённых значений
   - Вывод подтипов основан на эвристиках, не на онтологической иерархии

---

## Рекомендации по организации quadstore

### Рекомендуемый подход: Гибридная загрузка

#### 1. Загружать онтологию в отдельный граф

```turtle
vad:ontology {
    # Определения классов из vad-basic-ontology.ttl
    vad:Process rdf:type rdfs:Class ;
        rdfs:label "Process" ;
        rdfs:comment "Бизнес-процесс" .
    
    vad:VADProcessDia rdfs:subClassOf vad:TriG .
    # ... остальные определения
}
```

#### 2. Преимущества этого подхода

**SPARQL-Driven валидация**:
```sparql
# Проверка типа объекта через онтологию
ASK {
    GRAPH vad:ontology { ?type rdf:type rdfs:Class }
    GRAPH ?dataGraph { ?subject rdf:type ?type }
}
```

**Единый источник истины**:
- Все метаинформация доступна через SPARQL
- Не нужно дублировать константы в JavaScript

**Inference (вывод)**:
- Автоматическое включение подклассов в запросы
- Пример: "все TriG" автоматически включат VADProcessDia, ObjectTree

#### 3. Реализация

```javascript
async function loadOntology() {
    const response = await fetch('ontology/vad-basic-ontology.ttl');
    const ttlContent = await response.text();
    
    // Парсинг с добавлением в граф vad:ontology
    const ontologyQuads = await parseTtlWithGraph(ttlContent, 'vad:ontology');
    ontologyQuads.forEach(quad => currentStore.addQuad(quad));
}
```

#### 4. Исключение онтологии при сохранении

```javascript
function getQuadsForSave() {
    return currentStore.getQuads(null, null, null, null)
        .filter(quad => quad.graph.value !== 'http://example.org/vad#ontology');
}
```

---

## SPARQL-Driven подход

### Приоритет SPARQL над JavaScript

Согласно принципам проекта (`base_concept_rules.md`):

1. **Декларативный подход**:
   - Правила вывода в формате SPARQL CONSTRUCT
   - Запросы вместо императивного кода

2. **Единое хранилище**:
   - Все данные доступны через SPARQL
   - Включая онтологические определения

3. **Semantic Reasoning**:
   - Использование RDFS/OWL reasoning
   - Вместо жёстко заданных алгоритмов

### Пример SPARQL-Driven reasoning

```sparql
# Вычисление processSubtype через SPARQL CONSTRUCT
PREFIX vad: <http://example.org/vad#>

CONSTRUCT {
    ?process vad:processSubtype ?subtype .
    ?virtualTrig rdf:type vad:Virtual .
    ?virtualTrig vad:hasParentObj ?trig .
} WHERE {
    # Получаем метаданные процесса
    GRAPH vad:ptree {
        ?process rdf:type vad:TypeProcess .
        OPTIONAL { ?process vad:hasParentObj ?parent }
        OPTIONAL { ?process vad:hasTrig ?hasTrig }
    }
    
    # Определяем подтип через онтологию
    GRAPH vad:ontology {
        # Правила вывода через онтологическую иерархию
    }
    
    # Создаём виртуальный TriG
    BIND(IRI(REPLACE(str(?trig), "#t_", "#vt_")) AS ?virtualTrig)
}
```

---

## Что еще нужно загрузить в quadstore

### Минимальный набор (уже реализован)

1. ✅ **Технологическое приложение**:
   - `vad-basic-ontology_tech_Appendix.ttl` → `vad:techtree`
   - Предикаты для Smart Design
   - Стили визуализации

2. ✅ **Пользовательские данные**:
   - `vad:ptree`, `vad:rtree`
   - Схемы процессов `vad:t_*`

### Рекомендуемый набор (для SPARQL-Driven)

3. 🔄 **Базовая онтология**:
   - `vad-basic-ontology.ttl` → `vad:ontology`
   - Определения классов и иерархии
   - RDFS/OWL аксиомы для reasoning

4. 🔄 **Правила вывода**:
   - N3 правила для EYE-JS reasoner
   - SPARQL CONSTRUCT правила для Comunica

### Опциональный набор (для расширенного functionality)

5. 💡 **Примеры данных**:
   - `dia/example1.trig` → `vad:example1`
   - Для тестирования и демонстрации

6. 💡 **Вспомогательные онтологии**:
   - FOAF для исполнителей
   - DC/DCTERMS для метаданных

### Порядок загрузки

```javascript
async function initializeQuadstore() {
    // 1. Создаём хранилище
    currentStore = new N3.Store();
    
    // 2. Загружаем онтологию (рекомендуется)
    await loadOntology();  // vad:ontology
    
    // 3. Загружаем технологические данные
    await loadTechAppendix();  // vad:techtree
    
    // 4. Загружаем пользовательские данные
    await loadUserData();  // vad:ptree, vad:rtree, vad:t_*
    
    // 5. Выполняем reasoning
    await performInference(currentStore);
}
```

---

## Выводы

### Текущее состояние

- **✅ Работает**: Reasoning без онтологии через императивный JavaScript
- **✅ Работает**: Загрузка технологических данных
- **❌ Не реализовано**: Загрузка базовой онтологии в quadstore

### Рекомендации

1. **Краткосрочная** (совместимость):
   - Оставить текущую систему без изменений
   - Документировать ограничения

2. **Среднесрочная** (SPARQL-Driven):
   - Реализовать загрузку онтологии в `vad:ontology`
   - Перенести валидацию на SPARQL
   - Использовать semantic reasoning

3. **Долгосрочная** (полная семантизация):
   - Удалить JavaScript константы
   - Все правила через онтологию
   - Полный SPARQL-Driven подход

### Преимущества SPARQL-Driven подхода

- **Гибкость**: Легко добавлять новые классы и правила
- **Стандартность**: Использование W3C стандартов (RDFS, OWL, SPARQL)
- **Расширяемость**: Возможность использования внешних reasoner
- **Интеграция**: Совместимость с другими семантическими системами

---

**Автор**: AI Issue Solver  
**Проект**: RDF Grapher ver9d  
**Repository**: https://github.com/bpmbpm/rdf-grapher/tree/main/ver9d
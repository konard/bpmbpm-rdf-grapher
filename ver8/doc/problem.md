## problem
## role
на схеме процесса (TriG) нельзя ткнуть в объект Исполнитель

## id process
Непонятко как задавать id процесса: 
- руками указать номер,
- на основе родительского, т.е. добавить окончание
- в label менять пробелы на _

Как задавать иерархию? Пока задается через Схему (TriG).
Если нет схемы, то можно ввести еще признак Parent? ParentProcess. Радует, что сохраняется уникальность, т.к. все в дереве ptree и есть проверка на дубли.

Нужно синхронизировать Datailed - hasTriG

Может нужно только ParentProcess? ParentTriG - отменить?

Сделать иерархию Исполнителей и Процессов? 

## algorithm
Чтобы ввести новую схему, нужно иметь концепт процесса, т.к. именно на его основе формируется схема процесса.  
Допустим, что при создании нового концепта процесса будет вопрос - укажи материнский (proot).  
Основной вариант - когда мы создаем подпроцессы, т.е. знаем куда положить процесс (в какую корзину). Видимо такой путь: подпроцесс мы можем создать только к иеющемуся TriG. Типа: имеет материнский TriG и много - не материнских.  
Как вариант, можно указать с хх, т.е. 1.1.xx.1  


## auto

## copy
### Ключевые правила (запиши их в состав онтологии):
Добавлять триплеты мы можем только в какой-либо TriG, поэтому в окне Smart Design в поле TriG: нужно выбрать конкретный TriG. Кнопка «New TriG (VADProcessDia)» позволяет добавлять только TriG типа VADProcessDia. Другие типы TriG (новые TriG) не добавляются.
